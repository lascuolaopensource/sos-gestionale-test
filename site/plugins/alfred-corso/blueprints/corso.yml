title: Corso

tabs:
  #
  figure:
    label: Figure
    sections:
      #
      figure:
        headline: Figure
        type: fields
        fields:
          #
          docenti:
            label: Docenti
            type: pages
            query: site.find('utenti').children
            info: "{{ page.nascita_data }}"
            subpages: false
          #
          tutor:
            label: Tutor
            type: pages
            query: site.find('utenti').children
            info: "{{ page.nascita_data }}"
            subpages: false
          #
          coordinatori:
            label: Coordinatori
            type: pages
            query: site.find('utenti').children
            info: "{{ page.nascita_data }}"
            subpages: false
